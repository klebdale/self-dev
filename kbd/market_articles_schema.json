{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "article_title": {
      "type": "string"
    },
    "publisher": {
      "type": "string"
    },
    "date_published": {
      "type": "string",
      "format": "date"
    },
    "url": {
      "type": "string",
      "format": "uri"
    },
    "author": {
      "type": "string"
    },
    "article_summary": {
      "type": "string",
       "description": "A brief synopsis or summary of the article’s key points and takeaways."
    },
    "credibility_score": {
      "type": "number",
      "description": "A score representing the credibility of the article based on the publisher and the source."
    },
    "source_type": {
      "type": "string",
      "enum": ["news", "research", "social media", "blog", "opinion"]
    },
    "sentiment_score": {
      "type": "string",
      "enum": ["positive", "neutral", "negative"],
      "description": "The sentiment tone of the article's content."
    },
    "keywords": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Key topics or keywords covered in the article."
    },
    "content_type": {
      "type": "string",
      "enum": ["news", "case study", "interview", "opinion", "report", "analysis"],
      "description": "The specific type of content the article represents."
    },
  },
  "required": [
    "article_title",
    "publisher",
    "date_published",
    "url"
  ]
}
